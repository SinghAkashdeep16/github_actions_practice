name: "pr checks"

on:
  pull_request:
    types: [opened]
    branches: [main]       

jobs:                       
  check_pr:                 
    runs-on: ubuntu-latest  
    steps:                 
      - name: "check pr status and run tests"   
        run: echo "running pr checks ${{ github.ref_name }}"